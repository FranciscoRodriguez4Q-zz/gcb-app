import { DropdownOnEnterDirective } from './dropdown-on-enter.directive';

describe('DropdownOnEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownOnEnterDirective();
    expect(directive).toBeTruthy();
  });
});
