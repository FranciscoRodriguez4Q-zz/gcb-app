import { DoOnEnterDirective } from './do-on-enter.directive';

describe('DoOnEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new DoOnEnterDirective();
    expect(directive).toBeTruthy();
  });
});
